def check_password():
    # Sening koding shu yerda bo'ladi
    password = input("Parolni kiriting: ")
    if password == "admin123":
        print("Tizimga kirildi")
    else:
        print("Noto'g'ri parol!")

# Funksiyani chaqiramiz
check_password()
